# The node only needs `requests` on top of what ComfyUI already ships.
# torch, numpy, Pillow and av are deliberately NOT listed here: the ComfyUI Manager
# might reinstall them and break the CUDA build of torch in the process.
requests>=2.28
