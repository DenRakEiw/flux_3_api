[project]
# Immutable once published: this is the node's permanent registry id.
name = "flux-3-api"
description = "ComfyUI nodes for the BFL Flux 3 video API, with an OpenRouter-backed prompt helper."
version = "1.0.0"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["requests>=2.28"]

[project.urls]
Repository = "https://github.com/DenRakEiw/flux_3_api"
"Bug Tracker" = "https://github.com/DenRakEiw/flux_3_api/issues"

[tool.comfy]
PublisherId = "denrakeiw"
DisplayName = "Flux 3 API"
